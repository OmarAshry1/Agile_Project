# Supabase Database Configuration
# 
# TEAM SETUP INSTRUCTIONS:
# 1. Copy this file and rename it to 'database.local.properties'
# 2. Replace YOUR_PASSWORD_HERE with the actual Supabase database password
# 3. The database.local.properties file is already in .gitignore (won't be committed)
#
# Configuration Priority:
# 1. database.local.properties (your personal config)
# 2. Environment variables
# 3. Default values (below)

# Supabase Connection Details
# Get these from: Supabase Dashboard → Settings → Database → Connection string
supabase.host=db.sstcwguzxbkhjrsecrgq.supabase.co
supabase.port=6543
supabase.database=postgres
supabase.user=postgres
supabase.password=AinShams111

# Connection Pool Settings (optional - defaults are fine for most cases)
# Use connection pooler (port 6543) or direct connection (port 5432)
supabase.use.pooler=true

# Pool size settings
db.min.pool.size=2
db.max.pool.size=10

# Timeout settings (in milliseconds)
db.connection.timeout=30000
db.idle.timeout=600000
db.max.lifetime=1800000
db.leak.detection=60000



