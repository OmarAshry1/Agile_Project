package edu.facilities.model;

/**
 * Enum representing the type of a course attribute value (EAV pattern)
 */
public enum CourseAttributeType {
    TEXT,       // Text value
    NUMBER,     // Numeric value
    BOOLEAN,    // Boolean value (true/false)
    DATE,       // Date value
    JSON        // JSON object value
}

